ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
PORT=4317
HOST=127.0.0.1

# Optional password gate for a hosted/shared Node deployment (16+ characters).
APP_PASSWORD=
# Set these for HTTPS behind a trusted hosting proxy.
TRUST_PROXY=0
COOKIE_SECURE=0
APP_ORIGIN=
# Use a persistent mounted directory when hosting the Node server.
DB_PATH=data/one-more.sqlite
